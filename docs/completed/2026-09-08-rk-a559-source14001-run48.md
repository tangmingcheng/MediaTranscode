# 实际源 14001：H.264 1080p25 RTP → HEVC 1080p25 CBR 6 Mbps，run48

生产代码 `73ad5fbd`，CLI SHA-256 `7464cf735973a6f68d71a935e0f4a343bbff92ddd0615a8880451260e738f470`。保持 run47 全部 CLI 参数，使用用户指定实际源，无丢包注入。

容量修复与持续解码通过；完整无突发门禁 **FAIL**。用户明确要求先出库，再调查优化，因此此版本按已知限制交付，不标记完整验收成功。
## 实际命令

```bash
dir=/home/tang/MediaTranscode/out/acceptance/rk-a559-source14001-run48
/home/tang/MediaTranscode/out/build/rk-release/media_transcode_realtime_video_cli --media-id rk-userspace-low-h264720p30-hevc1080p25-cbr6m-v1 --egress-capacity-bps 50000000 --maximum-wire-residence-ms 100 --input-type rtp --output-layout mpegts --output-transport rtp --open-timeout-ms 30000 --read-timeout-ms 2000 --analyze-duration-us 5000000 --probe-size 5000000 --progress-timeout-ms 12000 --video-rtp-url rtp://192.168.130.229:61884 --video-rtp-codec h264 --video-rtp-payload-type 96 --video-rtp-clock-rate 90000 --rtp-host 192.168.96.122 --rtp-port 6200 --sdp "$dir/output.sdp" --video-codec hevc --rc cbr --bitrate 6000 --width 1920 --height 1080 --fps 25 --gop 50 --no-audio
```

源由用户给定 HTTP 接口启动和停止，本轮没有 FFmpeg 发流进程。实际调用参数如下；密钥仅脱敏展示，完整执行脚本保存在目标机证据目录，不提交凭据：

```text
http://192.168.130.229:15101/index/api/startSendRtp?secret=<实际密钥>&vhost=__defaultVhost__&app=rtp&stream=34020000001320014001_34020000001320014001&ssrc=3&dst_url=192.168.130.229&dst_port=61884&is_udp=1&use_ps=0&only_audio=0&src_port=0
http://192.168.130.229:15101/index/api/stopSendRtp?secret=<实际密钥>&vhost=__defaultVhost__&app=rtp&stream=34020000001320014001_34020000001320014001&ssrc=3&dst_url=192.168.130.229&dst_port=61884&is_udp=1&use_ps=0&only_audio=0&src_port=0
```

```powershell
D:\VideoLAN\VLC\vlc.exe --file-logging --log-verbose=2 --logfile=D:\Code\MyCode\MediaTranscode\out\acceptance\rk-a559-source14001-run48\vlc.log rtp://@192.168.96.122:6200
D:\Wireshark\dumpcap.exe -i 6 -f "udp and (port 6200 or port 6201)" -a duration:330 -w D:\Code\MyCode\MediaTranscode\out\acceptance\rk-a559-source14001-run48\receiver.pcapng -q
```

## 真实结果与失败原因

- 源 API 启动 2026-09-07 19:51:55.321837，停止 19:55:55.386693，均 code=0，持续 **240.064856 秒**。源期间 CLI 未提前退出。停源后 19:56:07.685 按既有 12 秒无进展超时退出，exit=1；不是源仍在运行时退出。
- 硬件链 h264_rkmpp → scale_rkrga async_depth=0 → hevc_rkmpp，zero_copy=true；VLC 默认 D3D11VA。源期间 93 个有效 GPU 解码计数样本逐次增长，连续 **228.3570752 秒**；该窗口无无效状态或不增长区间。
- 完整输入抓包 13,322 RTP，缺失/重复/乱序均0。运行约176秒时已捕获7个大于旧1400 B槽长的合法数据报，最大1402 B；本次 truncations=0、workerErrors=0、errors=0、droppedBuffers=0。run47 的“合法后续包超过样本峰值导致截断退出”已在相同真实源复现条件下消除。
- 接收槽长65507 B、64槽、数据区4192448 B，原有效预算4194304 B；接收区无运行后分配。此数字不是全会话内存上限。重排窗口随槽数降至64包，深乱序/20%输入丢包尚未用本版重新验证。
- 输出与接收 **140,575 RTP** 全量逐包一致，缺失/重复/乱序均0，TS错误为空；载荷摘要均 `55db6fce0d2fe4ee8475db1179c6265d1f8bbc4d63fd9b19458e3beb61d61ae3`。Windows dumpcap 没有保留最终接口丢包统计，不宣称该接口统计为0；逐包一致是本轮输出收发无丢包的依据。
- **失败原因证据：** 核心入内核队列服务曲线超额为单包1356 B；内核到驱动出现24包/179微秒聚集，超额31425.25 B；出口抓包对应24包/182微秒、seq50529..50552、超额31406.5 B。队列到驱动最大等待12.022毫秒、P99 51微秒。证明突发形成于入队后，具体阻塞/唤醒根因仍未定位，不能宣称核心发送通过就等于线速出口通过，也不能归咎于输入丢包。
- CPU平均单核13.102429%、峰值33.862434%；RSS初始24371200 B、峰值30466048 B，payload高水位42347023 B。无音频，A/V漂移不适用。

## 审查与适用范围

两名未参与实现的独立审查者对 `6fb07f19...73ad5fbd` 七个生产文件均明确 Standards PASS / Spec（容量修复）PASS。按 FFmpeg 完整UDP接收、GStreamer udpsrc 超预期MTU接收及既有有界RTP重排原则审查；完整无突发验收仍 FAIL。共享 Windows 容量路径已源码核对，按用户要求未进行 Windows 转码实测。此前生产版本2d597ba6的丢包矩阵不能外推为本版64槽丢包覆盖。

## PID 与证据

CLI4151765、源控制4151768、主脚本4151729、输入/输出tcpdump4151743/4151744、trace4151738、发送线程4151806；VLC30476、Windows dumpcap6508。测试结束已核实进程退出、目标机临时执行脚本删除、trace实例清理、lo无丢包注入。

目标机 `/home/tang/MediaTranscode/out/acceptance/rk-a559-source14001-run48/` 保存 CLI、源接口、输入/输出/接收pcap、packet/input/kernel/output-burst/datagram-size分析及实际脚本内容；本机同名目录保存VLC日志和GPU采样。运行脚本仅为临时诊断，无测试设施纳入仓库。源脚本按240秒调用停止接口，由源停止驱动CLI结束。