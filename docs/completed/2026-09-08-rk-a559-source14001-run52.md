# 实际源14001 run52：启动时已有20%输入丢包，未进入运行期恢复测试

生产代码73ad5fbd，原H.264 1080p25 RTP → HEVC 1080p25 CBR 6 Mbps参数，原命令与run51相同，仅目录run52。计划正常运行20秒后注入30秒20%输入丢包，但本轮未到注入阶段即探测失败，不标记恢复测试通过或运行期回归失败。

- 2026-09-08 09:43:56启动，09:44:07收尾。源API启动/停止均code=0。
- 核心报分析时限到达：matching_rtp_packets=240、matching_datagram_bytes=283290、parameter_sets=sps,pps。CLI exit1；检测核心退出后向源控制脚本发送SIGINT并停止源，控制脚本exit130。
- `input-qdisc-before.txt`明确记录启动时`loss 20%`，而loss-start.txt/loss-end.txt均不存在，脚本注入条件尚未触发。开始前较早一次检查为0%，谁在两次检查之间改变了设置未得到证据，不归因于任何人或进程。
- 输入pcap解析398个目标RTP，存在79次非连续序列转移；tcpdump捕获725个过滤包、捕获丢弃0。不能把本轮当作正常启动后再注入损伤的测试。
- 源码退出条件还要求帧率事实；SPS/PPS已存在不等于整个探测完成。重排在缺口处等待，而分析时限到达；当前错误文字不能单独证明缺参数集。未修改核心、未放宽时限。

目标机证据：out/acceptance/rk-a559-source14001-run52，包含启动qdisc快照、源结果、CLI日志、抓包、preflight-packet-analysis.json和实际脚本内容。主475994、CLI476030、源控制476035、输入/输出tcpdump476010/476011、VLC24736、Windows dumpcap24212均结束；临时目标机执行脚本删除、lo恢复0%。下一轮在启动点立即恢复并记录0%，随后仅在运行期注入损伤。

丢包/恢复命令仍为 `tc qdisc change dev lo root netem loss 20%` / `tc qdisc change dev lo root netem loss 0%`；该源在目标机本地转发，lo为输入路径，eth0输出未注入丢包。