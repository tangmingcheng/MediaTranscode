# HEVC 1440p30 RTP → H.264 1080p25 MPEG-TS/RTP VBR 5/12/13 Mbps：run57输入丢包恢复PASS

生产73ad5fbd，CLI SHA-256 `7464cf735973a6f68d71a935e0f4a343bbff92ddd0615a8880451260e738f470`。核心实际发送、同会话恢复、持续硬解及完整输出收发门禁通过。

## 实际命令

```bash
dir=/home/tang/MediaTranscode/out/acceptance/rk-a559-loss20-run57
/home/tang/MediaTranscode/out/build/rk-release/media_transcode_realtime_video_cli --media-id rk-userspace-high-hevc2k30-h2641080p25-vbr12m-v1 --egress-capacity-bps 50000000 --maximum-wire-residence-ms 100 --input-type rtp --output-layout mpegts --output-transport rtp --open-timeout-ms 30000 --read-timeout-ms 2000 --analyze-duration-us 5000000 --probe-size 5000000 --progress-timeout-ms 12000 --video-rtp-url rtp://192.168.130.229:61884 --video-rtp-codec hevc --video-rtp-payload-type 96 --video-rtp-clock-rate 90000 --rtp-host 192.168.96.122 --rtp-port 6200 --sdp "$dir/output.sdp" --video-codec h264 --rc vbr --min-bitrate 5000 --bitrate 12000 --max-bitrate 13000 --width 1920 --height 1080 --fps 25 --gop 50 --no-audio
ffmpeg -hide_banner -nostdin -re -i /home/tang/rk-highspec-hw-hevc2k30-248s.mp4 -map 0:v:0 -an -c:v copy -f rtp -payload_type 96 -ssrc 3 -sdp_file "$dir/input.sdp" 'rtp://192.168.130.229:61884?pkt_size=1400'
# 启动前0%，运行约20秒后注入，30.672788336秒后恢复
 tc qdisc change dev lo root netem loss 0%
 tc qdisc change dev lo root netem loss 20%
 tc qdisc change dev lo root netem loss 0%
```

```powershell
D:\VideoLAN\VLC\vlc.exe --file-logging --log-verbose=2 --logfile=D:\Code\MyCode\MediaTranscode\out\acceptance\rk-a559-loss20-run57\vlc.log rtp://@192.168.96.122:6200
D:\Wireshark\dumpcap.exe -i \Device\NPF_{9F7430E9-3A7B-4738-9F92-9B18AA0D18EC} -f "udp and (port 6200 or port 6201)" -a duration:310 -w D:\Code\MyCode\MediaTranscode\out\acceptance\rk-a559-loss20-run57\receiver.pcapng -q
```

## 结果与边界

- 已有源HEVC 2560×1440、30 fps、248.266667秒、11.5771 Mbps；FFmpeg仅复制包，核心hevc_rkmpp → scale_rkrga → h264_rkmpp，VLC默认D3D11VA。用户明确真实源无HEVC时允许该本地高规格源。输出1920×1080、25 fps、VBR 5/12/13 Mbps。
- 输入丢包15:16:35.812179593至15:17:06.484967929。损伤窗口收到25969 RTP、缺6529，pcap估计20.0905%；恢复后208513 RTP、196.601332秒，缺失/重复0、乱序8。
- 输入抓包自身drop211；注入前pcap缺106个RTP（序号8155至8260，15:16:22附近）。启动前qdisc为0%，核心首次RTP缺口日志在15:16:35.861注入后。证据支持此前缺口来自抓包遗漏，不能以这份输入pcap声称全程完整；损伤比例是观测估计。输出/接收捕获不受此限制，均drop0且全量匹配。
- 15:17:06.868收到完整关键帧，15:17:06.948同会话恢复输出，距恢复输入约0.463秒。损伤期间允许丢弃受损帧等待恢复。
- 恢复后99个有效GPU样本逐次增长，连续198.0083957秒（15:17:14.7956596至15:20:32.8040553），无无效状态；末段包含源EOF后的缓存视频硬解。
- 输出与接收266097 RTP逐包一致，缺失/重复/乱序0、TS错误为空；SHA-256同为`cc1855e9908bf0cc67bb242ee301d0c2f850e22a69268500e31cfb44f13a557b9`。双方抓包266160 UDP、drop0，包含维护尾部。
- 核心实际入队与驱动提交50 Mbps服务曲线超额均1356 B，最大内核排队114微秒。本轮PASS不代表历史驱动聚集已经修复。
- CPU平均单核17.581714%、峰60.913706%；RSS初始55640064 B、峰61272064 B；payload高水位13203666 B。workerErrors/errors/droppedBuffers/graphPayloadPressureFailures均0。无音频，A/V漂移不适用。
- 源自然EOF，15:20:24.659416151记录源结束、exit0；CLI随后无输入超时exit1，15:20:37.760349053收尾。源运行期间核心未退出。

## 证据与清理

目标机out/acceptance/rk-a559-loss20-run57保存脚本内容、probe/SHA、CLI/资源、输入/输出/接收pcap、input-phase、input-capture-gap、packet、kernel分析。本机同名目录保存VLC日志、GPU原始CSV/分析与接收捕获。

主515727、CLI515804、源515845、输入/输出抓包515786/515787已结束；发送TID515894。VLC32852关闭，dumpcap24824自然结束，临时脚本删除、trace清理、lo恢复0%。没有新增生产代码或仓库测试设施。本项独立提交PASS；73ad5fbd的四项RC/编码方向损伤恢复矩阵闭合，master合并仍等待完整独立交叉审查。