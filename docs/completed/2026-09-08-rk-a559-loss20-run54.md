# run54 环境准备失败，媒体链路未启动

计划HEVC 2K30 RTP → H.264 1080p25 MPEG-TS/RTP CBR6M输入丢包恢复，生产73ad5fbd。目标机SSH恢复后lo已回到noqueue，诊断脚本执行`tc qdisc change dev lo root netem loss 0%`报`Qdisc not found`并exit7，尚未启动CLI或源，因此没有pids.txt，不构成核心运行失败或通过。

已只读确认无CLI/源残留、trace实例清理；使用`tc qdisc replace dev lo root handle 30: netem loss 0%`建立输入损伤环境后，另起run55正式测试。原始script.log、源文件SHA和probe、实际脚本内容均保存在目标机out/acceptance/rk-a559-loss20-run54。临时执行脚本删除，Windows空链路VLC31348/dumpcap29432结束；不改核心或媒体参数。