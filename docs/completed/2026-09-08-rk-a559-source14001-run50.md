# 实际源14001 run50：目标机持续发送正常，接收验收证据不完整

生产代码73ad5fbd，原参数H.264 1080p25 RTP → HEVC 1080p25 CBR 6 Mbps，源240秒，无输入丢包注入。CLI/VLC实际命令见run48报告，仅证据目录替换为run50；目标机同名证据目录归档全部脚本内容。源由用户HTTP API提供，没有FFmpeg发流进程。

- API启动09:27:05.408762、停止09:31:05.510398，均code=0，持续240.101636秒。源期间核心无提前退出；停源后按既有12秒无进展超时收敛，CLI exit1、控制脚本exit0。
- 输入13401 RTP，缺失/重复0，乱序7；输出144380 RTP，缺失/重复/乱序0，TS错误为空。输入容量、硬解和核心运行未发现新的失败。
- 核心入队、驱动提交和出口抓包服务曲线超额均1356 B；队列等待最大94微秒。视频RTP落在队列1，本轮没有复现run48的队列0聚集，不能据此宣称其已修复。
- CPU平均单核12.932243%、峰值30.107527%；RSS初始24502272 B、峰值29958144 B；workerErrors/errors/droppedBuffers均0，payload高水位42996496 B。无音频，A/V漂移不适用。
- **完整验收证据不完整，不标记PASS。** Windows沿用重连前dumpcap接口6，实际已变为虚拟网卡“本地连接*10”，捕获0；192.168.96.122实际属于“以太网”，当前编号5、GUID 9F7430E9-3A7B-4738-9F92-9B18AA0D18EC。这是诊断配置错误，不是媒体接收丢失的证据。
- VLC日志明确D3D11VA，单次GPU计数有效，但启动时建立的全GPU持续查询未落盘有效样本，无法证明连续三分钟。修正为解码启动后按本进程实例重复查询并逐次落盘，run51启动后已确认计数增长与实际网卡抓包增长。

本轮函数跟踪证实stmmac_tx_timer由call_timer_fn调用，运行内核CONFIG_HZ=250；并非高精度定时器。当前tx-usecs=1000不能直接当作实际1毫秒完成时限。目标机头文件包含qdisc STATE_MISSED及内存屏障修复；不能仅按内核版本认定并发唤醒缺陷。上述仅缩小调查范围，没有证明队列0阻塞根因，未修改内核或核心。

PID：主467593、CLI467628、源控制467632、输入/输出抓包467607/467608、发送线程467669、VLC30400、Windows抓包22364。目标机进程结束、临时执行脚本删除；VLC关闭。源码未变化，已交付库不变。