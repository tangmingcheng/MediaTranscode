# 实际源14001 run49：源未上线，启动FAIL

生产代码73ad5fbd，沿用run48原始参数，H.264 1080p25 RTP → HEVC 1080p25 CBR 6 Mbps。CLI/VLC命令与run48报告一致，仅证据目录改为run49；实际脚本内容完整归档于目标机同名证据目录。计划240秒由用户API供源，未启动FFmpeg发流进程。

2026-09-08 09:13:25启动。startSendRtp返回code=-500、can not find the source stream；随后finally停止接口也返回code=-500。核心报 raw RTP video signaling probe reached open timeout before complete parameter sets，matching_rtp_packets=0、matching_datagram_bytes=0、parameter_sets=none。输入/输出pcap各0个捕获包，Windows dumpcap接收0、丢弃0，没有VLC解码窗口。

本轮失败根因：调用时指定源不存在，未发出RTP；不是容量回归或硬解失败，不修改核心。诊断脚本原等待上限比CLI探测超时短，因此cli.exit记录CLI_STILL_RUNNING_AFTER_SOURCE_END并发出SIGINT；实际终态错误以上述CLI日志为准，不冒称自然退出码。

09:26:23再次只读查询getMediaList已恢复两个schema，H.264 1920×1080@25、GOP50；因此接续run50同源同参数复测，无须更换源。run49不标记完整通过。

PID：主461594、CLI461624、源控制461628、输入抓包461608、输出抓包461609、VLC11316、Windows dumpcap18440。目标机进程均已结束，临时执行脚本删除，trace实例清理，VLC关闭；中断期间的空GPU采样无有效解码证据。没有测试代码或框架纳入仓库。