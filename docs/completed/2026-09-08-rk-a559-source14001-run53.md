# 实际源14001 H.264 1080p25 RTP → HEVC 1080p25 MPEG-TS/RTP CBR 6 Mbps，run53 输入丢包恢复 PASS

生产代码73ad5fbd，CLI SHA-256 `7464cf735973a6f68d71a935e0f4a343bbff92ddd0615a8880451260e738f470`。原媒体和CLI参数，64槽容量版本，正常启动后注入30秒20%输入丢包。同一会话恢复、持续硬解超过三分钟、完整收发一致、核心实际发送无突发，本项PASS。源提前结束的收尾异常单列，不伪装接口成功。
## 实际命令

```bash
dir=/home/tang/MediaTranscode/out/acceptance/rk-a559-source14001-run53
/home/tang/MediaTranscode/out/build/rk-release/media_transcode_realtime_video_cli --media-id rk-userspace-low-h264720p30-hevc1080p25-cbr6m-v1 --egress-capacity-bps 50000000 --maximum-wire-residence-ms 100 --input-type rtp --output-layout mpegts --output-transport rtp --open-timeout-ms 30000 --read-timeout-ms 2000 --analyze-duration-us 5000000 --probe-size 5000000 --progress-timeout-ms 12000 --video-rtp-url rtp://192.168.130.229:61884 --video-rtp-codec h264 --video-rtp-payload-type 96 --video-rtp-clock-rate 90000 --rtp-host 192.168.96.122 --rtp-port 6200 --sdp "$dir/output.sdp" --video-codec hevc --rc cbr --bitrate 6000 --width 1920 --height 1080 --fps 25 --gop 50 --no-audio
```

源由用户给定 HTTP 接口启动和停止，本轮没有 FFmpeg 发流进程。实际调用参数如下；密钥仅脱敏展示，完整执行脚本保存在目标机证据目录，不提交凭据：

```text
http://192.168.130.229:15101/index/api/startSendRtp?secret=<实际密钥>&vhost=__defaultVhost__&app=rtp&stream=34020000001320014001_34020000001320014001&ssrc=3&dst_url=192.168.130.229&dst_port=61884&is_udp=1&use_ps=0&only_audio=0&src_port=0
http://192.168.130.229:15101/index/api/stopSendRtp?secret=<实际密钥>&vhost=__defaultVhost__&app=rtp&stream=34020000001320014001_34020000001320014001&ssrc=3&dst_url=192.168.130.229&dst_port=61884&is_udp=1&use_ps=0&only_audio=0&src_port=0
```

```powershell
D:\VideoLAN\VLC\vlc.exe --file-logging --log-verbose=2 --logfile=D:\Code\MyCode\MediaTranscode\out\acceptance\rk-a559-source14001-run53\vlc.log rtp://@192.168.96.122:6200
D:\Wireshark\dumpcap.exe -i \Device\NPF_{9F7430E9-3A7B-4738-9F92-9B18AA0D18EC} -f "udp and (port 6200 or port 6201)" -a duration:310 -w D:\Code\MyCode\MediaTranscode\out\acceptance\rk-a559-source14001-run53\receiver.pcapng -q
```

## 丢包与恢复

```bash
# 启动CLI及源前恢复零丢包并记录快照
tc qdisc change dev lo root netem loss 0%
tc -s qdisc show dev lo
# 正常运行约20秒后，2026-09-08 10:26:11.628162210
tc qdisc change dev lo root netem loss 20%
# 30.565150788秒后，10:26:42.193312998
tc qdisc change dev lo root netem loss 0%
```

输入源在目标机本地转发，lo为输入路径；eth0出口没有注入丢包。启动CLI和源前的两份快照均无loss，修正run52前置环境不满足的问题。输入损伤窗口1454个已收RTP、320缺失，实际缺失18.0383%；正常前1218 RTP无缺失，恢复后10075 RTP无缺失、7次乱序。配置20%是随机概率，不要求每个有限窗口恰好20%。

## 真实结果

- 源启动10:25:51.311882，code=0。输入pcap持续230.799145秒，最后包10:29:42.110022；源先消失，计划240秒后的stopSendRtp返回code=-500、can not find the stream，控制脚本exit1。核心随后才无输入超时退出，10:29:56.320收尾、CLI exit1；没有源不停而核心先停。
- 恢复后10:26:43.672收到完整关键帧，10:26:43.713同一会话恢复输出（约1.52秒），workerErrors/errors/droppedBuffers均0。输入损伤期间会丢弃不能恢复的受损帧，不把该阶段当作无损播放。
- 硬件链h264_rkmpp → scale_rkrga → hevc_rkmpp、zero_copy=true，VLC默认D3D11VA。恢复后91个有效GPU计数样本逐次增长，**181.1904049秒**，10:26:51.7185012至10:29:52.9089061；无无效状态。末10.80秒包含源最后包之后仍在解码的已接收视频；不声称恢复后输入本身持续超过180秒（实际179.888407秒）。持续播放门禁以有效硬解计数验证。
- 输出与接收**121337 RTP**全量逐包一致，缺失/重复/乱序均0、TS错误为空，摘要同为`c91e105ac857c953581c084a511a20ed111e1c6b68ccf7e93eccfafba34988630`。Windows实际以太网抓包121392 UDP、drop0，完整覆盖停源后的维护尾部。
- 核心实际入队、驱动提交与出口抓包的50 Mbps服务曲线超额均为1356 B单包余量；最大队列等待105微秒。本项核心发送PASS，不据此宣称所有队列的既存内核聚集已修复。
- CPU平均单核10.246996%、峰值35.820896%；RSS初始25735168 B、峰值31580160 B，payload高水位42294522 B。无音频，A/V漂移不适用。

## 证据与清理

目标机out/acceptance/rk-a559-source14001-run53保存CLI、API、qdisc快照、输入/输出/接收pcap、input-phase/input-time/packet/kernel/output-burst分析及GPU分析；本机同名目录保存VLC日志、GPU CSV与接收pcap。脚本内容已归档，临时执行脚本删除。

主509391、CLI509425、源控制509430、输入/输出tcpdump509407/509408、发送线程509469、VLC28124、Windows dumpcap11828。测试进程均结束、trace实例清理、lo恢复0%。没有新增生产代码或测试设施。

仅本项实际H.264→HEVC CBR输入损伤恢复通过；新容量下HEVC→H.264 CBR与两项VBR矩阵仍待覆盖，原生产2d597ba6的通过项不外推。源码73ad5fbd双独立审查已PASS；本项形成独立提交与标记。